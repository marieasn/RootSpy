#  SConscript for plugin
#
#  *** rename this file to SConscript and place in every plugin directory ***
#
#  to customize fill in package dependencies
#
#  ejw, 16-apr-2013


Import('*')
from halld_lib import define_dependencies, buildPlugin

 
#  set package dependencies
define_dependencies(pluginEnv,'')


#  build the lib
buildPlugin(pluginEnv)
